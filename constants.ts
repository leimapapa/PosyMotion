
import { MotionConfig } from './types';

export const DEFAULT_CONFIG: MotionConfig = {
  delay: 3,
  opacity: 0.5,
  invert: true,
  hueShift: 0,
  brightness: 1.0,
  contrast: 1.0,
  grayscale: false
};

export const MAX_DELAY_FRAMES = 60;
